# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env.local
.env
.env.*.local

# Scratch workspace for subagent-driven execution ledgers
.superpowers/

# Toponym-spike output — MP3s to listen to, not artefacts to keep
apps/api/spike-audio/

# Provider A/B output. record-generation.ts writes here rather than over the
# golden fixtures when the model it just called is not the one that produced
# them — comparison material, not a committed baseline.
apps/api/cassettes/comparison/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
